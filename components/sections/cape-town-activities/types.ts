export type ActivityItem = {
  title: string;
  image: string;
  description: string;
  bestFor: string;
  idealBooking: string;
};

export type ServiceItem = {
  title: string;
  description: string;
  href: string;
  cta: string;
};

export type TravelerTypeItem = {
  title: string;
  text: string;
};